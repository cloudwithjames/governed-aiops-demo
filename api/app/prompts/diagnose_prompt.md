Investigate why the Customer Portal is unavailable.

Use the available MCP tools to:
1. check_portal - Check if the Customer Portal is returning errors
2. check_backend - Verify if the backend is healthy
3. get_gateway_logs - Look for gateway error logs
4. get_gateway_config - Check current gateway upstream configuration
5. get_recent_changes - Review recent configuration changes

After gathering evidence, determine the root cause and build a remediation plan.
Do NOT execute any fix. Stop after presenting the plan for human approval.

Cite all evidence IDs (E01, E02, etc.) in your root cause conclusion.

帮我检查 Customer Portal 为什么访问失败。

使用可用的 MCP 工具：
1. check_portal - 检查 Customer Portal 是否返回错误
2. check_backend - 验证后端是否健康
3. get_gateway_logs - 查看 Gateway 错误日志
4. get_gateway_config - 检查当前 Gateway 上游配置
5. get_recent_changes - 查看最近的配置变更

收集证据后，确定根因并构建修复计划。
不要执行任何修复。在提交计划等待人工审批后停止。

在根因结论中引用所有证据编号（E01, E02 等）。
