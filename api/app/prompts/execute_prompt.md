Execute only the approved remediation via apply_gateway_fix.
Then call verify_service.
Do not declare success unless verification passes.

Use the approval token provided. Do not invent or reuse tokens.
After verification, report the results.

仅执行已审批的修复操作，通过 apply_gateway_fix。
然后调用 verify_service。
在验证通过前不要声明成功。

使用提供的审批令牌。不要伪造或重复使用令牌。
验证后报告结果。
